if screenAt == "home" ->
    show the ImportBox.tsx.
if not:
    Show LogicTree.tsx SENDING THE `screenAt` contents within logictree somehow. 
    Use the sent content to open the "userData" + "something.dcrt" file, and render its content using map(). gl